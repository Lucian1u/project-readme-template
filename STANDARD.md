# GitHub 项目页标准

## 页面目标

一个第一次打开项目页的人，应在 30 秒内回答四个问题：

1. 这是什么？
2. 它解决我的什么问题？
3. 我现在怎么试？
4. 我凭什么相信它真的能用？

README 的顺序按这四个问题组织，不按开发者写代码的时间顺序组织。

## 固定结构

每个公开项目至少保留以下部分：

1. 项目名与一句话承诺
2. 一个主要行动入口
3. 项目状态
4. 为什么存在
5. 已实现的核心能力
6. 快速开始
7. 可复制的使用示例
8. 边界与限制
9. 开发与验收
10. License

下面这些章节按项目需要添加：

- 有可视结果：演示
- 有明确处理流程：工作原理
- 会产生文件或报告：交付内容
- 接触文件、账号、网络或密钥：隐私与安全
- 跨平台或跨工具：兼容性
- 仓库不止一个入口：仓库结构
- 接受外部贡献：参与项目、`CONTRIBUTING.md`、`SECURITY.md`

不适用的章节直接删掉，不保留空标题，也不写“敬请期待”。

Agent Skill 项目额外必填：

11. 可复制的安装入口

只给 ZIP 下载、解压路径或分步说明不够。打开仓库的人必须能复制一条命令、粘贴后完成安装。

## 内容规则

### 首屏只做三件事

- 一句话说清结果，不先讲技术栈
- 给出一个最重要的入口，最多再补一个次要入口
- 明确当前状态，避免把试验版写成稳定版

### 演示必须是证据

演示图或视频应直接呈现核心任务的输入、过程或结果。装饰性封面不能替代演示。没有合格演示时宁可不放。

### 所有命令都要跑过

安装、启动和验收命令必须在当前版本实际执行过。复制即报错的 Quick Start 比没有 Quick Start 更糟。

### Agent Skill 必须提供可复制安装入口

Skill 仓库的读者首先要能装上它。这段放在首屏承诺之后、详细说明之前，让打开页面不用往下翻就能复制。已有的分步安装（ZIP、各客户端目录）可以保留在后面，但不能替代这段。

单 Skill 仓库（根目录有 `SKILL.md`）使用下面这段，只替换仓库地址和目录名：

````markdown
## 安装

克隆到 Claude Code 的 skills 目录：

```bash
git clone https://github.com/{{OWNER}}/{{REPO}}.git \
  ~/.claude/skills/{{SKILL_NAME}}
```

或使用 Skills CLI，安装到当前检测到的 Agent：

```bash
npx skills add {{OWNER}}/{{REPO}} -g -y
```

安装后重启客户端。其他 Agent 环境可以直接把 [`SKILL.md`](SKILL.md) 作为 skill 入口加载。
````

写法规则：

- `{{SKILL_NAME}}` 必须与 `SKILL.md` frontmatter 里的 `name` 一致，不要让用户自己改文件夹名
- 命令必须完整，复制后不需要再拼路径
- 至少提供两条可复制命令：`git clone` 到 `~/.claude/skills/<name>`，以及 `npx skills add owner/repo -g -y`
- `git clone` 直装 Claude Code；`npx skills add -g -y` 是跨 Agent 的标准入口，会装到用户级的 Claude Code、Codex、Cursor 等目录，并跳过确认
- `-g` 与 `git clone` 一样装到用户级目录；`-y` 保证复制后可以直接执行，不要让用户再回答提示
- 多 Skill 合集不要把整个仓库克隆进单个 skill 目录；改用 `npx skills add owner/repo@skill-name -g -y`，或写明要复制的子目录
- 安装命令必须在当前版本实际执行过
- 不要只写“下载后放到 skills 目录”而不给可复制命令

### 把边界写在项目页上

说明项目不处理什么、哪些结果仍需人工判断、哪些环境没有验证。边界不是示弱，它能防止读者误用。

### 不用数字和徽章装饰可信度

只放能帮助判断或行动的徽章，例如 CI、版本和许可证。不要放没有决策价值的访问计数、语言占比或装饰徽章，也不要写未经核验的性能数字。

## GitHub 页面配套

README 之外，每个公开项目还应检查：

- About 描述与 README 的一句话承诺一致
- 有在线入口时填写 Website
- 添加 5–10 个真正相关的 Topics
- 选择并提交明确的 `LICENSE`
- 稳定版本使用 tag、Release 和 `CHANGELOG.md`
- 有视觉界面时设置 Social preview；没有合格图片就不设置
- 需要接收反馈时再开启 Issues，并提供最小问题模板

## 隐私检查

发布前确认仓库和图片中没有：

- 本名、私人邮箱、电话、住址或证件信息
- 本机绝对路径、用户名、设备名或内部目录结构
- API Key、token、cookie、验证码或登录二维码
- 私有仓库地址、内部文档链接或带权限参数的临时 URL
- 聊天记录、日志、截图元数据里无意带出的私人信息

公开昵称、公开项目和明确用于公开联系的社交账号可以保留。拿不准的信息先不发布。

## 完成标准

一个项目页只有同时满足下面条件才算完成：

- 不看源码也能说清项目用途和目标用户
- 主要入口在首屏可见且链接有效
- Agent Skill 项目有可直接复制的安装命令，且命令已在当前版本执行过
- Quick Start 已在当前版本跑通
- 至少一个示例能得到描述中的结果
- 核心能力都能在仓库、演示或验收记录中找到证据
- 未实现内容没有写成已实现
- 边界、隐私和未验证环境写清楚
- 全部 `{{...}}` 占位符已经替换
- 无本名、私人联系方式、本机路径或凭证
- README、About、Topics、Website、License 和版本状态互相一致

## 推荐的发布前搜索

在仓库根目录检查残留占位符和常见隐私信息：

```bash
rg -n '\{\{|/Users/|/home/|[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}|api[_-]?key|token|cookie' .
```

搜索结果需要人工判断；它能发现明显风险，但不能代替逐页检查。
